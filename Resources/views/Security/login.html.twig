{% extends 'AppBundle:Layout:main.html.twig' %}
{% block content %}
<div style="width: 30%; margin: auto">
    <div class="well no-padding">
        <form action="{{ path('fos_user_security_login') }}" id="login-form" class="smart-form client-form" novalidate="novalidate">
            <header>JKN Executive Dashboard</header>
            <fieldset>
                <section>
                    <label class="label">Username</label>
                    <label class="input"> <i class="icon-append fa fa-user"></i>
                        <input type="email" name="_username">
                    </label>
                </section>
                <section>
                    <label class="label">Password</label>
                    <label class="input"> <i class="icon-append fa fa-lock"></i>
                        <input type="password" name="_password">
                    </label>
                </section>
                <section>
                    <label class="checkbox">
                        <input type="checkbox" name="remember" checked="">
                        <i></i>Selalu ingat saya</label>
                </section>
            </fieldset>
            <footer>
                <button type="submit" class="btn btn-primary">Login</button>
            </footer>
        </form>
    </div>
</div>
{% endblock %}